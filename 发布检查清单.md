# 血栓检测小程序 · 发布前检查清单

> **生成日期**：2026-08-13
> **依据**：三端全栈代码审计 + 29 个独立对抗性验证任务（所有 P0/P1 均已 CONFIRMED，无推翻项）
> **说明**：本清单仅用于检查勾选，不包含任何代码修改。

## 使用说明

- `- [ ]` 未完成；`- [x]` 已完成（在编辑器中把 `[ ]` 改为 `[x]` 即可勾选）
- 每项均标注具体位置（文件:行号，可点击跳转）
- 优先级：**P0** = 发布即事故，必须修复后才能发布；**P1** = 高危，强烈建议发布前修复；**P2** = 建议修复

---

## 一、P0 阻断问题（必须修复后才能发布）

### 1.1 微信登录未实现 —— 所有用户共用同一个假 openid

> 风险：生产环境下所有用户映射为同一账号，任意用户登录后即获得他人账号的 token，订单、用药、检测结果全部互相可见（账号接管级别事故）。

- [ ] 实现 `jscode2session` 调用，用真实 `wx.login` code 换取 openid
- [ ] 删除 `openid = openidPrefix + "stable"` 硬编码逻辑
- [ ] 生产配置 `openid-prefix` 必须为空且不再被引用

**位置**：[AuthService.java:43-48](backend/src/main/java/com/thrombosis/service/AuthService.java#L43)

### 1.2 默认启动 profile 为 dev —— mock 支付/固定验证码可能直接带上生产

> 风险：生产若按 README 方式直接启动（未指定 profile），将启用固定验证码 123456 和模拟支付回调。

- [ ] 生产启动显式指定 `--spring.profiles.active=prod`（部署脚本/文档固化）
- [ ] 确认生产环境 `thrombosis.dev.mock.payment=false`、`verify-code` 为空且不生效

**位置**：[application.yml:8](backend/src/main/resources/application.yml#L8)、[application-dev.yml:20-22](backend/src/main/resources/application-dev.yml#L20)

### 1.3 测试账号无条件写入每一个环境（包括生产）

> 风险：`admin/admin123`、`hospital_admin/admin123`、`13800000000/123456` 三个已知密码账号在生产也存在，任何人可用默认密码登录后台。

- [ ] `DataInitializer` 增加 `@Profile("dev")` 或等效环境判断
- [ ] 生产数据库确认不存在这三个默认账号（如已存在需删除/改密）

**位置**：[DataInitializer.java:29-71](backend/src/main/java/com/thrombosis/config/DataInitializer.java#L29)

### 1.4 支付：无真实微信支付、无签名校验、mock 回调无归属校验

> 风险：任何登录用户可伪造任意订单（含他人订单）为已支付并取得核销码；生产无真实支付链路。

- [ ] 实现真实微信支付下单 + 支付回调接口（APIv3 签名验签）
- [ ] 支付回调必须幂等 + 校验订单归属 + 校验金额与订单一致
- [ ] mock 回调增加订单归属校验，或直接下线 `/api/payment/mock-callback`
- [ ] 生产环境确认 mock 支付开关为 false

**位置**：[PaymentController.java:30](backend/src/main/java/com/thrombosis/controller/PaymentController.java#L30)、[OrderService.java:119-155](backend/src/main/java/com/thrombosis/service/OrderService.java#L119)

### 1.5 核销二维码接口公开且可按订单 ID 枚举

> 风险：订单 ID 自增，任何人遍历 `/api/orders/1/qrcode`、`/2/qrcode`… 可获取全部用户的核销码 PNG。

- [ ] `/api/orders/{id}/qrcode` 增加鉴权 + 订单归属校验（或改为签名短链）
- [ ] 从 `WebMvcConfig` 的公开放行列表中移除该路径

**位置**：[OrderController.java:63-80](backend/src/main/java/com/thrombosis/controller/OrderController.java#L63)、[WebMvcConfig.java:26](backend/src/main/java/com/thrombosis/config/WebMvcConfig.java#L26)

---

## 二、P1 高危问题（强烈建议发布前修复）

### 2.1 `verify/check` 泄露他院订单信息

> 任何医护输入一个核销码即可看到不属于本院的订单号、套餐、脱敏手机号。

- [ ] `check()` 增加跨医院校验（与 `confirm()` 一致，见 [VerifyService.java:67](backend/src/main/java/com/thrombosis/service/VerifyService.java#L67)）

**位置**：[VerifyService.java:33-55](backend/src/main/java/com/thrombosis/service/VerifyService.java#L33)

### 2.2 医护工作台「待处理数」未按医院过滤

> `pendingCount` 查的是全库已支付订单，与注释「本院」矛盾，泄露全院数据。

- [ ] 查询增加 `.eq(Orders::getHospitalId, hospitalId)`

**位置**：[StaffService.java:60-62](backend/src/main/java/com/thrombosis/service/StaffService.java#L60)

### 2.3 核销 confirm 非原子 —— 可并发重复核销

> 先查状态再更新，两个并发请求可同时通过判断，重复写核销记录、重复发消息。

- [ ] 加行锁（`SELECT ... FOR UPDATE`）/ 乐观锁 / 数据库唯一约束（如 `verify_record` 加 `uk(order_id)`）

**位置**：[VerifyService.java:70-84](backend/src/main/java/com/thrombosis/service/VerifyService.java#L70)

### 2.4 小程序配置整体停留在开发态

- [ ] `BASE_URL` 改为生产 HTTPS 域名（当前 `http://localhost:8080`）
- [ ] `DEV.requestPaymentMock` 改为 `false`
- [ ] 填入 5 个真实订阅消息模板 ID（当前全为空）
- [ ] `DEV.locationMock` 改为 `false`

**位置**：[config/index.js:7,12,14,18-24](miniapp/config/index.js#L7)

### 2.5 定位/隐私合规缺失 —— 真机上 `wx.getLocation` 直接失败

- [ ] `app.json` 增加 `requiredPrivateInfos: ["getLocation"]`
- [ ] 增加 `__usePrivacyCheck__` 并在公众平台配置《用户隐私保护指引》
- [ ] 修复真实定位下 `city` 恒为空的问题（页面拿不到城市）

**位置**：[app.json:66-70](miniapp/app.json#L66)、[location.js:14](miniapp/utils/location.js#L14)

### 2.6 JWT 密钥安全

> 生产默认空串：未注入 `JWT_SECRET` 时全部登录崩掉；注入弱密钥时 token 可伪造。dev 密钥已硬编码进仓库。

- [ ] 生产注入 ≥32 字节强随机 `JWT_SECRET`（部署文档固化）
- [ ] 轮换并移除 dev 硬编码密钥，改用环境变量注入

**位置**：[application-prod.yml:13](backend/src/main/resources/application-prod.yml#L13)、[application-dev.yml:15](backend/src/main/resources/application-dev.yml#L15)、[JwtUtil.java:23](backend/src/main/java/com/thrombosis/security/JwtUtil.java#L23)

---

## 三、P2 中低危问题（建议修复，按优先级排序）

### 后端

- [ ] 生产验证码链路：`verify-code` 为空时注册接口 `@NotBlank` 校验导致**所有用户无法注册**（当前无真实短信实现）— [application-prod.yml:21](backend/src/main/resources/application-prod.yml#L21)、[AuthService.java:63](backend/src/main/java/com/thrombosis/service/AuthService.java#L63)
- [ ] 验证码加频率限制、有效期、错误次数上限；移除明文日志 — [AuthService.java:61-64](backend/src/main/java/com/thrombosis/service/AuthService.java#L61)
- [ ] CORS 收紧：当前 `*` + 允许凭据，生产应只允许管理端域名 — [CorsConfig.java:18-21](backend/src/main/java/com/thrombosis/config/CorsConfig.java#L18)
- [ ] 医护/管理员登录校验账号 `status`（禁用账号当前仍可登录）— [StaffService.java:32](backend/src/main/java/com/thrombosis/service/StaffService.java#L32)、[AdminService.java:45](backend/src/main/java/com/thrombosis/service/AdminService.java#L45)
- [ ] 订单状态机统一：文档定义 `paid→verified→completed`，但核销直接 `paid→completed`，`verified` 状态不可达 — [VerifyService.java:81](backend/src/main/java/com/thrombosis/service/VerifyService.java#L81)
- [ ] 检测结果双插入竞态：`test_result.order_id` 加唯一索引 — [schema.sql:105](backend/src/main/resources/db/schema.sql#L105)、[ResultService.java:90-99](backend/src/main/java/com/thrombosis/service/ResultService.java#L90)
- [ ] 销量自增改原子更新（当前读-改-写，并发丢更新）— [OrderService.java:145](backend/src/main/java/com/thrombosis/service/OrderService.java#L145)
- [ ] 更新用药方案会重新生成时间点 ID，导致历史打卡记录被孤立 — [MedicationService.java:113](backend/src/main/java/com/thrombosis/service/MedicationService.java#L113)

### 小程序

- [ ] 分页 `finished` 判断错误（reset 时旧 list 长度重复计入，提前截断）— [order-list.js:63](miniapp/pages/order-list/order-list.js#L63)、[record.js:54](miniapp/pages/staff/record/record.js#L54)、[messages.js:49](miniapp/pages/messages/messages.js#L49)
- [ ] 核销码输入框：数字不渲染到格子 — [verify-code-input.js:26](miniapp/components/verify-code-input/verify-code-input.js#L26)
- [ ] 核销确认按钮无防重入（连点可双核销）— [scan.js:56](miniapp/pages/staff/scan/scan.js#L56)
- [ ] 订单详情「去支付」在生产环境无真实支付分支，点击静默无响应 — [order-detail.js:70](miniapp/pages/order-detail/order-detail.js#L70)
- [ ] 角色守卫只包了 `onShow`，`onLoad` 会先跑 API 再跳转 — [guard.js:11](miniapp/utils/guard.js#L11)
- [ ] `request.js` 忽略 HTTP 状态码、只看 body.code — [request.js:32](miniapp/utils/request.js#L32)
- [ ] 登录失败兜底 `dev_code` 硬编码上线 — [login.js:33](miniapp/pages/login/login.js#L33)
- [ ] 结果列表忽略 `orderId` 参数，显示全部结果 — [result-list.js:18](miniapp/pages/result-list/result-list.js#L18)
- [ ] 订单列表「再次购买」按钮绑定了不存在的方法 — [order-list.wxml:33](miniapp/pages/order-list/order-list.wxml#L33)

### 管理端（admin）

- [ ] 医院端套餐占比饼图字段名与后端不匹配，恒为空 — [dashboard/index.vue:36](admin/src/views/dashboard/index.vue#L36)
- [ ] 医院端「本院订单数」`orderCount` 从未赋值，恒为 0 — [sales/index.vue:84](admin/src/views/sales/index.vue#L84)
- [ ] 退款数 `refundCount` 硬编码 0 — [sales/index.vue:60](admin/src/views/sales/index.vue#L60)
- [ ] 医院端把非金额计数卡片误用 hide-money 显示为 `-` — [dashboard/index.vue:8](admin/src/views/dashboard/index.vue#L8)
- [ ] 核销导出 CSV 表头与数据列数不一致 — [verify/index.vue:81](admin/src/views/verify/index.vue#L81)
- [ ] 平台端核销表「医院」「核销人」显示原始 ID 而非名称 — [verify/index.vue:28](admin/src/views/verify/index.vue#L28)
- [ ] 账单重新同步无条件提示成功 — [bills/index.vue:79](admin/src/views/bills/index.vue#L79)
- [ ] 医院管理员标题硬编码「北京协和医院」— [SideBar.vue:4](admin/src/components/SideBar.vue#L4)
- [ ] 套餐保存无封面/轮播图时产生 `['']` 脏数据 — [packages/Edit.vue:117](admin/src/views/packages/Edit.vue#L117)

---

## 四、微信平台侧发布条件（在公众平台 mp.weixin.qq.com 完成）

### 4.1 账号与资质

- [ ] 使用已完成**主体认证**的真实 AppID（当前 `wxfabb33c3ba42a0a2`，需确认归属与认证状态）
- [ ] 完成**小程序 ICP 备案**
- [ ] 申请**医疗/健康类目**并准备对应资质：《医疗机构执业许可证》/互联网医院资质或类目授权（血栓检测+用药管理属医疗健康类目，无资质大概率审核不通过）

**位置**：[project.config.json:51](miniapp/project.config.json#L51)（AppID 配置）；资质文件仓库中不存在，需线下准备

### 4.2 服务器域名与 HTTPS

- [ ] 后端部署到**已备案服务器**，配置有效 HTTPS 证书
- [ ] 在公众平台「开发管理 → 开发设置 → 服务器域名」添加 request 合法域名（必须 https）
- [ ] 配置微信支付回调域名
- [ ] `BASE_URL` 替换为上述生产域名

**位置**：[config/index.js:7](miniapp/config/index.js#L7)；管理端域名占位 [.env.production:3](admin/.env.production#L3)

### 4.3 微信支付

- [ ] 申请**商户号 mchid** + APIv3 密钥/证书
- [ ] 开通对应**支付类目资质**
- [ ] 注入 `WX_MCH_ID`、`WX_NOTIFY_URL` 等环境变量（当前全部为空占位）

**位置**：[application-prod.yml:18-19](backend/src/main/resources/application-prod.yml#L18)

### 4.4 订阅消息

- [ ] 在公众平台申请 5 个订阅消息模板（支付成功/核销成功/检测结果/用药提醒/套餐推送）
- [ ] 将真实模板 ID 填入配置（当前全空，订阅静默假成功）

**位置**：[config/index.js:18-24](miniapp/config/index.js#L18)、[subscribe.js:18-20](miniapp/utils/subscribe.js#L18)

### 4.5 隐私合规

- [ ] 填写《用户隐私保护指引》（收集手机号、定位、健康数据须逐项声明）
- [ ] 登录/注册页《用户协议》《隐私政策》当前仅为 `<text>` 无跳转，需提供真实页面链接 — [login.wxml:27](miniapp/pages/login/login.wxml#L27)、[register.wxml:39](miniapp/pages/register/register.wxml#L39)
- [ ] 手机号获取评估是否改用 `wx.getPhoneNumber`（当前为手输 + 验证码）— [register.wxml:20-29](miniapp/pages/register/register.wxml#L20)

### 4.6 其他平台配置

- [ ] `urlCheck` 关闭状态确认：上传后正式环境强制校验合法域名，`urlCheck:false` 仅影响开发工具 — [project.config.json:8](miniapp/project.config.json#L8)、[project.private.config.json:6](miniapp/project.private.config.json#L6)
- [ ] 统一基础库版本（当前两处不一致：3.5.5 vs 3.17.0），并在目标版本下做真机回归 — [project.config.json:50](miniapp/project.config.json#L50)、[project.private.config.json:2](miniapp/project.private.config.json#L2)
- [ ] `sitemap.json` 收紧：用药、检测结果等隐私页面设为 `disallow`（当前 `allow *` 全站收录）— [sitemap.json:3-8](miniapp/sitemap.json#L3)
- [ ] （可选）自定义药名/昵称等自由文本接入 `msgSecCheck` 内容安全检测 — [medication-edit.wxml:14](miniapp/pages/medication-edit/medication-edit.wxml#L14)、[register.wxml:13](miniapp/pages/register/register.wxml#L13)

---

## 五、后端生产部署清单

- [ ] 部署真实 MySQL 并执行 `schema.sql`（注意：脚本会 **DROP 后重建**全部表，勿在生产已有数据时直接执行）
- [ ] 清理 `seed.sql` 的测试数据（示例订单/核销记录/账单，避免污染生产）
- [ ] 注入全部环境变量：`MYSQL_HOST/MYSQL_DB/MYSQL_USER/MYSQL_PASSWORD`、`JWT_SECRET`（≥32字节）、`WX_APPID/WX_SECRET/WX_MCH_ID/WX_NOTIFY_URL`
- [ ] 启动时显式指定 `--spring.profiles.active=prod`
- [ ] 确认生产无测试账号、无 mock 端点可用

**位置**：[application-prod.yml:5-24](backend/src/main/resources/application-prod.yml#L5)、[schema.sql:1-8](backend/src/main/resources/db/schema.sql#L1)

---

## 六、发布前最终验证步骤（真机）

- [ ] 真机预览（非开发者工具）走通全流程：微信登录 → 注册 → 选套餐 → 下单 → **真实支付** → 查看核销码
- [ ] 医护端真机验证：登录 → 扫码核销 → 出具结果 → 用户端收到消息与结果
- [ ] 越权回归测试：跨医院核销被拒、跨用户订单/结果/消息不可见、未登录访问受限
- [ ] 支付回归：重复回调幂等、金额校验、退款流程（如涉及）
- [ ] 订阅消息真机弹窗与推送验证
- [ ] 定位授权流程验证（拒绝授权后的降级体验）
- [ ] 提审前用「体验版」截图准备审核材料（含医疗资质文件）

---

## 附：问题统计

| 级别 | 数量 | 说明 |
|------|------|------|
| P0 阻断 | 5 | 假 openid 登录、dev 默认 profile、测试账号进生产、无真实支付/无签名、核销码可枚举 |
| P1 高危 | 6 | 跨院信息泄露×2、并发重复核销、小程序开发态配置、定位/隐私合规、JWT 密钥 |
| P2 建议 | 约 30 | 见第三部分（后端 8 / 小程序 10 / 管理端 9，另有部分未列出的低优项） |

**底线结论**：当前项目是"可演示的开发态"，尚未达到可发布状态。P0 全部修复 + 第四、五章全部完成勾选后，才具备提审条件。
