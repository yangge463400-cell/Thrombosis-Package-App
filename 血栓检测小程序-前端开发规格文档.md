# 血栓检测服务 · 微信小程序前端开发规格文档

> 版本：v1.0 ｜ 面向：前端 AI 生成引擎 ｜ 说明：本文档为小程序前端开发的唯一规格依据，生成代码时请严格按页面清单、交互逻辑、接口约定与异常处理实现。

---

## 1. 项目概述

### 1.1 项目背景
面向需要血管健康检测与抗凝治疗管理的用户，提供「血栓检测套餐购买 → 医院核销 → 检测结果查询 → 抗凝用药管理」全流程服务。包含用户端、医护核销端两个微信小程序 + 管理端 Vue Web 后台。

### 1.2 角色与权限矩阵

| 角色 | 所属端 | 核心权限 |
|------|--------|---------|
| 用户 user | 用户端小程序 | 登录注册、购买套餐、查看核销码、查医院、查询结果、用药管理、消息中心、个人档案 |
| 医生/医护 staff | 医护核销端小程序 | 扫码核销用户核销码、查看本院核销记录、检测后为患者标记血栓状态 |
| 医院管理员 hospital_admin | 管理端 Vue Web | 本院套餐售卖查看、本院核销记录管理 |
| 平台管理员 admin | 管理端 Vue Web | 全医院核销/售卖、套餐管理（上下架、配置价格）、支付账单（微信+支付宝同步）、医院管理 |

> 用户端与医护端为**两个独立小程序**（用户扫码与医护扫码的场景互斥）；**管理端前端形态为 Vue（Web 后台）**，与小程序共用同一套后端接口（`/api/admin/*`），登录态与请求封装独立（见 2.7），页面结构与路由见第 5 章。

### 1.3 工程结构（两套：小程序 + Vue 管理端）

**A. 小程序工程（用户端、医护端，原生微信小程序，结构一致）：**

```
project/
├── app.js                 # 全局逻辑：登录态检查、全局数据
├── app.json               # 页面注册、tabBar、window 配置
├── app.wxss               # 全局样式、主题变量
├── project.config.json
├── sitemap.json
├── utils/
│   ├── request.js         # 统一请求封装（含 token、401 刷新、错误提示）
│   ├── auth.js            # 登录、token 管理
│   ├── format.js          # 时间、金额格式化
│   ├── subscribe.js       # 订阅消息申请封装
│   └── location.js        # 定位与城市选择封装
├── components/            # 公共组件（见 2.3）
├── pages/                 # 页面（按端划分）
├── services/              # 业务接口模块
└── images/                # 静态资源（tab 图标、banner、占位图）
```

**B. 管理端工程（Vue Web 后台）：**

```
admin/
├── src/
│   ├── main.js            # 入口：Vue 3 + Vite + Pinia + Element Plus（或自建组件）
│   ├── App.vue            # 布局：侧边菜单 + 顶栏 + 内容区（按角色显示菜单）
│   ├── router/index.js    # 路由（见 5.1），含登录守卫
│   ├── api/
│   │   ├── request.js     # axios 封装：BASE_URL、token 注入、401 拦截、错误提示
│   │   └── modules/       # 业务接口模块（admin/packages/verify/...）
│   ├── stores/            # Pinia：auth（登录态/角色/医院范围）、app（菜单/配置）
│   ├── views/             # 页面组件（对应 5.1 路由）
│   ├── components/        # 公共组件（表格、筛选、统计卡等）
│   ├── utils/             # 格式化、权限指令 v-permission
│   └── styles/            # 主题变量（可与小程序 2.1 配色保持一致）
├── vite.config.js
├── package.json
└── .env.development / .env.production   # 环境切换
```

---

## 2. 全局设计规范

### 2.1 主题与视觉规范
- **主色** `#1677FF`（医疗蓝）：按钮、tabBar 选中、导航、链接
- **辅助色** `#52C41A`（健康绿）：成功状态、已核销、检测完成
- **警示色** `#FF4D4F`：价格强调、异常、未读红点、删除
- **提醒色** `#FAAD14`：待支付、提醒类
- **背景色** `#F5F7FA`，卡片白底圆角 `16rpx`，阴影轻微
- 字号规范（适老化大字体系）：页面标题 `44rpx`/加粗、卡片标题 `40rpx`、正文 `36rpx`、辅助 `32rpx`、最小可读 `28rpx`；按钮文字 ≥ `36rpx`
- 圆角/间距：统一使用 `8/16/24/32rpx` 间距体系（列表行距与卡片内边距整体放大）
- 安全区：底部操作栏需适配 `env(safe-area-inset-bottom)`

#### 适老化（中老年用户）专项规范 —— 本项目强制要求
本项目核心用户为中老年人，以下为**硬性要求**，生成所有页面时必须遵守：
1. **字号最低标准**：正文 ≥ `36rpx`，辅助说明 ≥ `32rpx`，任何文字不得小于 `28rpx`；关键信息（价格、核销码、用药剂量、订单状态）用 `44rpx`+ 大字加粗
2. **文字对比度**：正文 `#333333`；辅助文字不得低于 `#666666`；禁止用浅灰（#999 及以下）承载重要信息
3. **点击热区**：所有可点击元素（按钮/列表项/开关/打卡项）高度 ≥ `88rpx`，相邻间距 ≥ `16rpx`，防误触
4. **按钮**：主按钮高度 ≥ `96rpx`、全圆角 `48rpx`、文字 ≥ `36rpx`；重要操作建议「文字+图标」双表达
5. **表单输入**：输入框高度 ≥ `88rpx`、字体 `36rpx`、placeholder 用 `32rpx` 深灰
6. **列表与卡片**：列表行高 ≥ `120rpx`，卡片内边距 ≥ `24rpx`，降低信息密度，一屏内容宁少勿多
7. **数字信息**：核销码、金额、剂量、时间等数字使用 ≥ `60rpx` 大号字，便于老年人现场出示与核对
8. **图标**：功能图标 ≥ `56rpx`、tabBar 图标 ≥ `60rpx`，均配文字标签
9. **红点角标**：未读红点直径 ≥ `24rpx`，位置醒目
10. **确认弹窗**：弹窗按钮文字 ≥ `36rpx`，取消/确认按钮间距充足
11. **大字模式**（可选增强）：预留「一键大字」切换（root-font-size / page-meta 方案），默认开启大字

### 2.2 tabBar 配置（仅用户端）
4 个 tab：**首页 / 检测套餐 / 消息 / 我的**
- 首页：`pages/index/index`，icon 为"房子"
- 检测套餐：`pages/package-list/package-list`，icon 为"药箱/检测"
- 消息：`pages/messages/messages`，icon 为"铃铛"（需支持未读红点角标，用 tabBar badge 或自定义导航）
- 我的：`pages/profile/profile`，icon 为"人像"
- 选中色 `#1677FF`，未选中 `#999999`；图标需提供 81x81px PNG 两态

### 2.3 公共组件清单
| 组件 | 用途 | 关键属性 |
|------|------|---------|
| `price-display` | 金额展示 | `value`、`size`、`color`，统一保留两位小数，¥ 符号 |
| `empty-state` | 空状态占位 | `icon`、`text`、`btnText`（可选重试/去购买） |
| `count-down` | 验证码 60s 倒计时 | `seconds`、`onFinish` |
| `verify-code-input` | 核销码/验证码输入 | 6 位数字、明文展示、自动跳格 |
| `filter-bar` | 筛选栏（地区/检测项目/排序） | 弹层式筛选面板 |
| `tag` | 状态/标签 | 类型：`success/warning/danger/default` |
| `package-card` | 套餐卡片 | 名称、价格、项目标签、医院数、销量 |
| `order-status-steps` | 订单状态时间线 | `steps[]`、`current` |
| `medication-record-item` | 用药打卡项 | 时间点、剂量、状态（已服/未服） |
| `message-item` | 消息列表项 | 类型图标、标题、摘要、时间、未读态、长按删除 |
| `stepper` | 数量/剂量步进器 | `min/max/step/value` |
| `bottom-bar` | 底部固定操作栏 | 按钮组、价格展示、安全区适配 |
| `nav-bar` | 自定义导航栏（部分页面） | 标题、返回、右侧操作 |

### 2.4 统一请求封装（utils/request.js）
```javascript
// 全局配置：环境切换 dev/prod，域名需在小程序后台配置 request 合法域名（HTTPS）
const BASE_URL = 'https://api.c-platform.com/miniapp/v1';

const request = (options) => new Promise((resolve, reject) => {
  const token = wx.getStorageSync('access_token');
  wx.request({
    url: `${BASE_URL}${options.url}`,
    method: options.method || 'GET',
    data: options.data || {},
    header: { 'Content-Type': 'application/json', Authorization: token ? `Bearer ${token}` : '', ...options.header },
    success: (res) => {
      // 后端统一返回 { code, message, data }；code === 0 视为成功
      if (res.data && res.data.code === 0) return resolve(res.data.data);
      if (res.statusCode === 401) { // token 失效 → 清登录态 → 回登录页
        wx.removeStorageSync('access_token');
        wx.navigateTo({ url: '/pages/login/login' });
        return reject({ code: 401 });
      }
      wx.showToast({ title: res.data?.message || '请求失败', icon: 'none' });
      reject(res.data);
    },
    fail: () => { wx.showToast({ title: '网络异常，请检查网络', icon: 'none' }); reject({ code: -1 }); },
  });
});

// 接口模块示例（services/package.js）
const getPackages = (params) => request({ url: '/packages', data: params });
module.exports = { getPackages };
```

### 2.5 登录态与权限
- 登录成功后在本地缓存 `access_token`、`user_info`、`is_registered`
- 每个 tab 页 `onShow` 检查登录态：未登录 → 跳登录页
- 接口 401 统一拦截（见 request.js）
- 页面内需要身份的操作（提交订单、保存用药、查看核销码）均需登录态

### 2.6 消息订阅封装（utils/subscribe.js）
- 封装 `wx.requestSubscribeMessage`：申请订阅消息权限，返回 `{ accepted: [], denied: [] }`
- 订阅模板（占位，ID 由 C 平台后台配置）：
  - 支付成功通知 / 核销成功通知 / 检测结果通知 / 用药提醒 / 套餐推送提醒
- 触发时机（转化率最高点申请）：
  - 提交订单后 → 申请「支付成功」「核销成功」「检测结果」
  - 保存用药后 → 申请「用药提醒」「套餐推送」

### 2.7 管理端（Vue）工程规范
- **技术栈**：Vue 3（组合式 API）+ Vite + Vue Router 4 + Pinia + Element Plus（推荐，表格/表单/弹窗开箱即用）；图表用 ECharts（vue-echarts）
- **请求封装（api/request.js）**：axios 实例，统一 `BASE_URL`、`Authorization` 注入、401 跳登录、统一错误提示；响应结构与小程序约定一致 `{ code, message, data }`，`code===0` 成功
- **登录态**：token 存 localStorage，路由守卫（`router.beforeEach`）校验未登录跳 `/login`；登录后按角色（admin / hospital_admin）动态生成菜单并控制数据范围
- **权限**：封装 `v-permission` 指令（如医院管理员隐藏"医院管理"入口）；接口层由后端按角色过滤数据
- **UI 一致性**：主题色沿用 2.1（`#1677FF` 主色、`#52C41A` 成功、`#FF4D4F` 警示）；后台为桌面端操作场景，字号可维持常规 Web 标准（14-16px），但统计数字、金额等核心数据用大号加粗
- **移动端兼容**（可选）：管理端若需手机访问，页面布局需响应式适配（表格横向滚动/卡片化），并提供 `viewport` 配置

---

## 3. 用户端小程序（血栓检测服务）

### 3.1 页面总览

| # | 页面 | 路径 | 是否 tab | 功能 |
|---|------|------|:---:|------|
| 1 | 登录 | `pages/login/login` | | 微信授权登录、异常重试 |
| 2 | 注册 | `pages/register/register` | | 手机号+验证码注册、自动登录 |
| 3 | 首页 | `pages/index/index` | ✅ | Banner、功能入口、推荐套餐、进行中订单 |
| 4 | 检测套餐列表 | `pages/package-list/package-list` | ✅ | 筛选（地区/检测项目）、分页列表 |
| 5 | 套餐详情 | `pages/package-detail/package-detail` | | 项目/价格/适用人群/医院范围、立即购买 |
| 6 | 订单确认 | `pages/order-confirm/order-confirm` | | 套餐信息、总价、提交订单 |
| 7 | 订单列表 | `pages/order-list/order-list` | | 状态 tab、订单卡片、操作按钮 |
| 8 | 订单详情 | `pages/order-detail/order-detail` | | 状态时间线、核销码、支付 |
| 9 | 合作医院查询 | `pages/hospital-list/hospital-list` | | 定位城市、医院列表、地图/电话 |
| 10 | 检测记录 | `pages/result-list/result-list` | | 检测记录列表、状态 |
| 11 | 检测结果详情 | `pages/result-detail/result-detail` | | 报告指标、免责声明 |
| 12 | 用药管理列表 | `pages/medication-list/medication-list` | | 当前用药、今日打卡 |
| 13 | 用药登记/编辑 | `pages/medication-edit/medication-edit` | | 药物/剂量/多时间点/提醒 |
| 14 | 用药详情 | `pages/medication-detail/medication-detail` | | 打卡记录、历史、修改 |
| 15 | 消息中心 | `pages/messages/messages` | ✅ | 分类、未读标红、长按删除 |
| 16 | 消息详情 | `pages/message-detail/message-detail` | | 正文、类型化跳转、标记已读 |
| 17 | 个人中心 | `pages/profile/profile` | ✅ | 头像昵称、档案、功能入口 |
| 18 | 个人档案编辑 | `pages/profile-edit/profile-edit` | | 性别/年龄/身高/体重 |

---

### 3.2 页面详细规格

#### 页面 1：登录页 `pages/login/login`
- **入口**：小程序启动（app.js 检测无 token 时）、所有需登录操作跳转
- **功能**：微信授权登录，兼容"已注册 / 未注册"两分支
- **UI 结构**：
  - 顶部品牌区：Logo + 产品名 + slogan
  - 协议区：勾选「我已阅读并同意《用户协议》《隐私政策》」（默认勾选，未勾选点击登录时提示）
  - 主按钮：「微信一键登录」
  - 异常态提示区（隐藏态，条件展示）：「未授权无法使用小程序」+「重新授权」按钮
- **交互逻辑**：
  1. `wx.login()` 获取 code → 调用 `POST /api/auth/wechat-login { code }`
  2. 后端返回：`{ isRegistered: true, token, user }` 或 `{ isRegistered: false, registerTicket }`
  3. 已注册 → 存 token/user → `wx.switchTab` 到首页
  4. 未注册 → 携带 `registerTicket` 跳转注册页
  5. **头像昵称**：按微信新规，使用「头像昵称填写能力」——头像用 `<button open-type="chooseAvatar">`、昵称用 `<input type="nickname">`，在注册页/个人中心完成，登录页不做强制获取
- **接口**：`POST /api/auth/wechat-login`
- **异常处理**：
  - 用户拒绝授权 / `wx.login` fail → 展示「未授权无法使用小程序」+「重新授权」按钮
  - 接口调用失败 → toast「登录失败，请重试」，按钮恢复可点击

#### 页面 2：注册页 `pages/register/register`
- **入口**：登录页未注册分支跳转
- **功能**：手机号 + 验证码注册，注册后自动关联微信账号并完成登录
- **UI 结构**：
  - 手机号输入框（11 位数字校验）
  - 验证码输入框 +「获取验证码」按钮（60s 倒计时，复用 `count-down`）
  - 头像昵称区域（可选补充：chooseAvatar + nickname input）
  - 协议勾选（复用登录页状态）
  - 主按钮：「注册并登录」
- **交互逻辑**：
  1. 获取验证码：`POST /api/auth/send-code { phone }`，成功开启倒计时
  2. 提交：`POST /api/auth/register { registerTicket, phone, code, nickname?, avatar? }`
  3. 成功 → 存 token → `wx.switchTab` 首页
- **接口**：`send-code` / `register`
- **异常处理**：
  - 验证码错误 → toast「验证码错误，请重新输入」
  - 手机号格式错误 → 输入框内提示「请输入正确的手机号」
  - 重复注册 → 后端提示「该手机号已注册，请直接登录」→ 回登录页
  - 保存失败 → toast「信息保存失败，请重试」

#### 页面 3：首页 `pages/index/index`（tab）
- **入口**：tab 默认页
- **功能**：产品门面 + 功能分流 + 进行中订单状态卡
- **UI 结构**：
  - 顶部搜索栏（跳套餐列表并携带关键字，可选）
  - Banner 轮播（swiper，运营位，可跳套餐详情/活动页）
  - 功能入口宫格：检测套餐、我的订单、检测记录、用药管理、消息中心、合作医院、帮助与客服
  - 进行中订单卡：最近一笔「待检测/待支付」订单，显示状态 + 操作按钮（去支付/查看核销码/前往检测）
  - 推荐套餐横向卡片（滑动）：套餐名、价格、销量
- **交互逻辑**：`onShow` 拉取 `GET /api/home`（banner、入口角标数、推荐套餐、进行中订单）；消息入口显示未读红点（`GET /api/messages/unread-count`）
- **接口**：`GET /api/home`、`GET /api/messages/unread-count`
- **异常处理**：接口失败展示重试按钮，不阻塞页面渲染

#### 页面 4：检测套餐列表 `pages/package-list/package-list`（tab）
- **入口**：tab / 首页宫格 / 消息跳转
- **功能**：按「地区 + 检测项目」筛选查找套餐
- **UI 结构**：
  - 顶部筛选栏 `filter-bar`：地区（省/市 联动选择）、检测项目（多选 chips）、排序（默认/价格↑/价格↓/销量）
  - 套餐卡片列表（`package-card`）：封面图、名称、检测项目 tags、价格（¥ 红色）、合作医院数量、已售数量
  - 触底分页加载（`onReachBottom`），空状态用 `empty-state`（「暂无符合条件的套餐」+ 清除筛选按钮）
- **交互逻辑**：
  1. 页面 `onLoad` 默认拉取 `GET /api/packages { page, pageSize, city?, itemIds?, sort? }`（`itemIds` 为检测项目**名称**，逗号分隔，套餐 items 以 name 关联）
  2. 筛选面板确认后重置分页并重新请求
  3. 点击卡片 → `navigateTo` 套餐详情（携带 `id`）
- **接口**：`GET /api/packages`、`GET /api/dicts/items`（检测项目字典）、`GET /api/dicts/cities`（城市字典）
- **异常处理**：加载失败 → 空态重试；无结果 → 空态文案

#### 页面 5：套餐详情 `pages/package-detail/package-detail`
- **入口**：套餐列表 / 消息跳转（携带 `id`）
- **功能**：套餐完整信息展示 + 购买入口
- **UI 结构**：
  - 轮播图（swiper）
  - 价格区：价格（¥ 大字红色）
  - 检测项目列表：项目名 + 说明（icon 列表）
  - 适用人群（tags）
  - 合作医院范围（城市列表 + 「查看合作医院」入口）
  - 购买须知/服务协议（折叠面板）
  - 底部操作栏 `bottom-bar`：「立即购买」按钮（主色大按钮）
- **交互逻辑**：
  1. `onLoad` 拉取 `GET /api/packages/:id`
  2. 未登录点击「立即购买」→ 跳登录页；已登录 → 跳订单确认（携带 `id`）
- **接口**：`GET /api/packages/:id`
- **异常处理**：套餐已下架 → 展示「套餐已下架」禁用按钮；接口失败 → 空态重试

#### 页面 6：订单确认 `pages/order-confirm/order-confirm`
- **入口**：套餐详情「立即购买」
- **功能**：核对套餐信息、总价，提交订单并拉起微信支付
- **UI 结构**：
  - 套餐信息卡（封面、名称、检测项目、适用人群）
  - 金额区：套餐原价、优惠（可选）、应付总额（大字）
  - 底部栏：「提交订单」→ 拉起支付
- **交互逻辑**：
  1. `POST /api/orders { packageId }` 创建订单 → 返回 `orderId` 及支付参数
  2. 成功后申请订阅消息（支付成功/核销/检测结果模板）
  3. `wx.requestPayment`（timeStamp/nonceStr/package/paySign 由后端返回）
  4. 支付成功 → toast「支付成功」→ 跳订单详情（或「购买成功」页）
  5. 支付取消 → 保留订单，跳订单详情展示「待支付」
- **接口**：`POST /api/orders`（创建并返回支付参数）
- **异常处理**：支付失败/取消 → toast「支付失败，请重新支付」，订单保留在「待支付」；重复提交 → 前端按钮 loading 防抖

#### 页面 7：订单列表 `pages/order-list/order-list`
- **入口**：个人中心「我的订单」/ 首页
- **功能**：按状态查看订单
- **UI 结构**：
  - 状态 tab：全部 / 待支付 / 待检测 / 检测中 / 已完成 / 已取消
  - 订单卡片：套餐缩略图、名称、金额、状态 tag、时间；每张卡片下方操作按钮（按状态）
  - 分页加载 + 空态
- **交互逻辑**：
  - 待支付 → 「去支付」「取消订单」
  - 已支付 → 「查看核销码」（跳订单详情定位到核销码）「前往检测」（跳医院查询）
  - 已完成 → 「查看结果」「再次购买」
  - 点击卡片 → 订单详情
- **接口**：`GET /api/orders { status, page, pageSize }`、`POST /api/orders/:id/cancel`
- **异常处理**：取消失败 → toast 后端 message

#### 页面 8：订单详情 `pages/order-detail/order-detail`
- **入口**：订单列表点击 / 支付成功后跳转
- **功能**：订单全貌 + 核销码 + 状态流转
- **UI 结构**：
  - 状态区：`order-status-steps` 时间线（已下单 → 已支付 → 已核销 → 已完成）
  - 核销码卡（已支付后显示）：**超大字核销码（6 位，≥ `60rpx`，明暗切换显示）+ 有效期**；说明文字 ≥ `32rpx`「到医院出示核销码，医护扫码核销」
  - 套餐信息、金额明细、支付方式、订单号、下单时间
  - 操作按钮（按状态同上）
- **交互逻辑**：
  - 「前往检测」→ 医院查询页（携带城市参数）
  - 核销码明暗切换用 `switch` 图标；核销后状态自动刷新
- **接口**：`GET /api/orders/:id`
- **异常处理**：核销码加载失败 → 重试；订单不存在 → 返回列表

#### 页面 9：合作医院查询 `pages/hospital-list/hospital-list`
- **入口**：订单详情「前往检测」/ 套餐详情「查看合作医院」/ 首页
- **功能**：按城市筛选就近合作医院
- **UI 结构**：
  - 顶部城市选择（定位当前城市 + 城市列表选择器）
  - 医院卡片列表：医院名、地址、联系电话、可检测时间（如「周一至周六 8:00-11:30」）、距离（可选）
  - 卡片操作：「导航」（`wx.openLocation`）、「拨打电话」（`wx.makePhoneCall`）
- **交互逻辑**：
  1. 进入页面调用 `wx.getLocation`（需在页面明确展示用途文案 + 隐私授权）
  2. `GET /api/hospitals { city, lat, lng }`（支持按距离排序）
  3. 选择医院后如从订单进入，可回写订单核销医院（可选，后端处理）
- **接口**：`GET /api/hospitals`
- **异常处理**：定位失败 → 默认展示城市选择器，不阻塞；无医院 → 空态

#### 页面 10：检测记录 `pages/result-list/result-list`
- **入口**：个人中心 / 消息跳转
- **功能**：历史检测记录与状态
- **UI 结构**：记录卡片：套餐名、检测日期、医院名、状态 tag（待检测 / 已核销 / 检测中 / 已出结果 / 结果异常）、点击进详情
- **接口**：`GET /api/results`（分页）
- **异常处理**：加载失败 → 空态重试

#### 页面 11：检测结果详情 `pages/result-detail/result-detail`
- **入口**：检测记录 / 消息通知跳转
- **功能**：查看检测报告
- **UI 结构**：
  - 报告头部：套餐名、检测日期、医院、报告编号
  - 指标列表：指标名、数值、参考范围、异常标记（超出范围红色高亮）
  - 顶部醒目免责声明：「本检测结果仅用于健康管理参考，不构成医疗诊断，如有疑问请咨询医生」
  - 底部操作：「联系客服」「再次购买套餐」（可选）
- **交互逻辑**：如后端提供报告 PDF/图片 URL → 提供「查看完整报告」webview 或下载
- **接口**：`GET /api/results/:id`
- **异常处理**：结果未同步 → 空态「检测结果暂未更新，请稍后重试」+ 刷新按钮

#### 页面 12：用药管理列表 `pages/medication-list/medication-list`
- **入口**：个人中心「用药管理」/ 首页宫格 / 消息跳转
- **功能**：查看当前用药、今日打卡
- **UI 结构**：
  - 顶部提示卡：医生评估状态（检测完成后医生标记「已确认血栓/正常」，若确认血栓 → 展示「建议购买抗凝药物效果检测套餐」banner 按钮）
  - 当前用药卡片：药物名、剂量（XXmg/次，XX次/天）、时间点列表、提醒开关（switch）
  - 「今日打卡」区：各时间点按钮（未服 → 点击变已服，绿色）
  - 底部：「+ 新增用药」
- **交互逻辑**：`onShow` 拉取 `GET /api/medications`；提醒开关切换调用 `PUT /api/medications/:id`
- **接口**：`GET /api/medications`
- **异常处理**：无用药记录 → 空态 + 新增按钮

#### 页面 13：用药登记/编辑 `pages/medication-edit/medication-edit`
- **入口**：用药列表「新增」/ 用药详情「编辑」
- **功能**：登记/修改用药方案
- **UI 结构**：
  - 药物名称：常用抗凝药选择（华法林、利伐沙班、达比加群酯、阿哌沙班、依诺肝素等 chips）+ 自定义输入
  - 剂量：每次剂量 `input`（XX mg/次）+ 每日次数 `stepper`（XX 次/天）
  - 服用时间点：可添加多个（默认 08:00、20:00），每个时间点用 `picker mode="time"`，支持删除
  - 提醒开关（订阅消息：用药提醒模板）
  - 底部：「保存」
- **交互逻辑**：
  1. 校验：药物名必填、剂量>0、至少 1 个时间点
  2. 保存前申请订阅消息「用药提醒」「套餐推送」
  3. `POST /api/medications`（新增）或 `PUT /api/medications/:id`（编辑）
  4. 保存成功 → 返回列表，toast「保存成功，提醒时间将按新设置执行」
- **接口**：`POST /api/medications`、`PUT /api/medications/:id`
- **异常处理**：保存失败 → toast「信息保存失败，请重试」；订阅拒绝 → 提示「提醒可能无法送达，可在消息页重新开启」

#### 页面 14：用药详情 `pages/medication-detail/medication-detail`
- **入口**：用药列表点击
- **功能**：用药方案详情 + 打卡记录
- **UI 结构**：
  - 方案信息卡（药物、剂量、时间点、起止时间）
  - 近 7 日打卡日历/列表：每天各时间点状态（已服 ✓ / 未服 ○）
  - 操作：「标记今日全部已服」（快捷）、「编辑」「删除」
- **交互逻辑**：点击单个时间点 → `POST /api/medications/:id/records { timePoint }` 标记已服；删除需 `wx.showModal` 二次确认
- **接口**：`GET /api/medications/:id`、`POST /api/medications/:id/records`、`DELETE /api/medications/:id`
- **异常处理**：打卡失败 → toast 重试；删除失败 → 保持现状提示

#### 页面 15：消息中心 `pages/messages/messages`（tab）
- **入口**：tab / 首页宫格
- **功能**：全部消息查看、已读/未读、删除
- **UI 结构**：
  - 分类 tab：全部 / 订单 / 用药 / 检测 / 系统
  - 消息列表（`message-item`）：类型图标、标题、摘要、时间；**未读消息标题红色加粗 + 红点**；点击 → 消息详情并标记已读；**长按 → 删除确认弹窗**
- **交互逻辑**：
  1. `onShow` 拉取 `GET /api/messages { type?, page }`
  2. 未读数驱动 tabBar badge 与首页红点
  3. 删除 → `DELETE /api/messages/:id`（本地不存消息，纯服务端同步）
- **接口**：`GET /api/messages`、`DELETE /api/messages/:id`
- **异常处理**：删除失败 → toast；分页加载失败 → 重试

#### 页面 16：消息详情 `pages/message-detail/message-detail`
- **入口**：消息中心点击
- **功能**：消息正文 + 类型化跳转
- **UI 结构**：标题、时间、正文（富文本/图文）、底部操作按钮（按类型）
- **交互逻辑**：
  - `onLoad` 调 `PUT /api/messages/:id/read` 标记已读
  - 类型化跳转：检测结果 → 检测结果详情；用药提醒 → 用药管理；套餐推送 → 套餐详情；订单 → 订单详情
- **接口**：`GET /api/messages/:id`、`PUT /api/messages/:id/read`
- **异常处理**：消息不存在 → 返回列表

#### 页面 17：个人中心 `pages/profile/profile`（tab）
- **入口**：tab
- **功能**：用户信息、档案、功能入口聚合
- **UI 结构**：
  - 头部用户卡：头像（chooseAvatar 可改）、昵称（nickname input 可改）、手机号（脱敏展示）
  - 个人档案卡：性别 / 年龄 / 身高 / 体重，编辑入口
  - 功能列表（cell 列表）：我的订单、检测记录、用药管理、消息中心（红点）、合作医院、帮助与客服、关于我们
  - 底部：「退出登录」
- **交互逻辑**：`onShow` 刷新用户信息与未读数；退出登录 → 清 token → 跳登录页
- **接口**：`GET /api/user/profile`、`POST /api/auth/logout`
- **异常处理**：信息加载失败 → 重试

#### 页面 18：个人档案编辑 `pages/profile-edit/profile-edit`
- **入口**：个人中心档案卡
- **功能**：填写/修改性别、年龄、身高、体重
- **UI 结构**：性别（radio：男/女）、年龄（input 数字）、身高 cm（input）、体重 kg（input）、底部「保存」
- **交互逻辑**：校验范围（年龄 1-120、身高 50-250cm、体重 10-300kg）；`PUT /api/user/profile`；保存成功 toast「档案已更新」并返回
- **接口**：`PUT /api/user/profile`
- **异常处理**：校验失败 → 对应输入框提示；保存失败 → toast 重试
- **合规提示**：页面底部小字「档案信息仅用于检测项目匹配与医护参考，严格保密」

---

## 4. 医护核销端小程序（血栓检测医护端）

> 独立小程序，供医院医护扫码核销使用。**不做微信支付、不做用户注册。**

### 4.1 页面总览

| # | 页面 | 路径 | 功能 |
|---|------|------|------|
| 1 | 医护登录 | `pages/login/login` | 账号密码/手机号登录，绑定医院 |
| 2 | 工作台 | `pages/index/index` | 扫码核销入口、今日统计 |
| 3 | 扫码核销 | `pages/scan/scan` | 扫码、校验订单、确认核销 |
| 4 | 核销结果 | `pages/verify-result/verify-result` | 成功/失败反馈 |
| 5 | 核销记录 | `pages/record/record` | 本院核销记录、按日筛选 |
| 6 | 检测结果上传 | `pages/result-upload/result-upload` | 上传检测结果（预留，见 9.2 法律风险） |

### 4.2 页面详细规格

#### 医护登录 `pages/login/login`
- 手机号 + 验证码 / 账号密码登录；登录后绑定 `hospitalId`、`staffId`、`role`（医生/护士）
- 接口：`POST /api/staff/login`
- 异常：密码错误 → toast「账号或密码错误」；无权限 → 提示「无核销权限，请联系管理员」

#### 工作台 `pages/index/index`
- 顶部医护信息 + 所属医院
- **大按钮「扫码核销」**（主色，居中醒目）
- 今日统计卡：今日核销数 / 待处理
- 入口：「核销记录」「检测结果上传」
- 接口：`GET /api/staff/statistics`

#### 扫码核销 `pages/scan/scan`
- 核心逻辑：
  1. `wx.scanCode()` 扫描用户核销码（二维码内容为核销码字符串或 URL）
  2. `POST /api/verify/check { code }` 校验：返回订单信息（套餐名、用户脱敏手机号、订单状态、有效期）
  3. 页面展示确认卡：「确认核销？」→ `POST /api/verify/confirm { code, hospitalId }`
  4. 支持手动输入核销码（`verify-code-input`，备用）
- 异常处理：
  - 已核销 → 提示「该核销码已被使用」
  - 已过期/无效 → 提示「核销码无效或已过期」
  - 核销接口失败 → 「核销失败，请联系医院工作人员或客服」
  - 扫码失败/取消 → 返回工作台

#### 核销结果 `pages/verify-result/verify-result`
- 成功态：绿色 ✓ 动画、套餐名、用户、核销时间、「完成」返回工作台
- 失败态：红色 ✗、失败原因、操作：「重新核销」「手动输入」

#### 核销记录 `pages/record/record`
- 日期选择 + 列表：核销时间、套餐、用户、核销人；分页
- 接口：`GET /api/verify/records { date, page }`

#### 检测结果上传 `pages/result-upload/result-upload`（预留）
- 医院端上传检测结果文件/数据，需后端提供上传接口与鉴权
- **重要：检测结果上传涉及医疗数据合规，上线前需确认法律风险并取得用户授权，见 9.2**

---

## 5. 管理端（Vue Web 后台 · 平台/医院管理员）

> 前端形态：**Vue 3 + Vite 单页应用**（见 2.7 工程规范），供平台管理员与医院管理员使用。平台管理员可看全医院数据，医院管理员仅看本院数据（后端按角色过滤）。与小程序共用同一套后端接口（`/api/admin/*`），工程独立部署。

### 5.1 路由总览（vue-router）

| # | 页面 | 路由路径 | 功能 |
|---|------|---------|------|
| 1 | 管理员登录 | `/login` | 账号登录 |
| 2 | 工作台 | `/dashboard` | 数据总览（销售额、订单、核销） |
| 3 | 套餐列表 | `/packages` | 全量套餐、上下架切换 |
| 4 | 套餐编辑 | `/packages/edit/:id?` | 新增/编辑套餐、**配置价格**（无 id 为新增） |
| 5 | 核销管理 | `/verify` | 全医院核销记录、按医院/日期筛选 |
| 6 | 售卖管理 | `/sales` | 全医院售卖记录、销售额统计 |
| 7 | 支付账单 | `/bills` | 微信+支付宝支付账单、对账状态 |
| 8 | 医院管理（可选） | `/hospitals` | 合作医院增删改、可检测时间 |

> 布局：`App.vue` 采用「左侧菜单（按角色动态生成）+ 顶栏（用户信息/退出）+ 内容区」后台布局；医院管理员菜单仅含 工作台/核销管理/售卖管理（数据自动限定本院）。

### 5.2 页面详细规格

#### 管理员登录（`/login`）
- 账号密码登录；登录后区分 `admin` / `hospital_admin` 角色并据此控制菜单与数据范围
- 实现：表单校验（Element Plus form）+ 登录接口 + 路由守卫放行；登录失败 toast「账号或密码错误」
- 接口：`POST /api/admin/login`

#### 工作台（`/dashboard`）
- 统计卡：今日销售额、今日订单数、待核销数、累计售出（金额/数字大号加粗展示）
- 图表（ECharts）：近 7 日销售额折线图、各医院售卖占比饼图（可选）
- 快捷入口：套餐管理、核销管理、售卖管理、支付账单、医院管理
- 接口：`GET /api/admin/statistics`

#### 套餐列表（`/packages`）
- 表格：封面缩略图、名称、价格、状态 tag（售卖中/已下架）、销量
- 操作：行内「上架/下架」switch 实时切换（`PUT /api/admin/packages/:id/status`）；行点击/「编辑」→ 套餐编辑；右上「+ 新增套餐」
- 顶部搜索：名称关键字 + 状态筛选；分页
- 接口：`GET /api/admin/packages`（分页 + 关键字搜索）

#### 套餐编辑（`/packages/edit/:id?`）
- 表单字段：名称、**价格（¥，管理员配置）**、封面图（上传组件，axios FormData 提交）、轮播图、检测项目（多选 + 自定义）、适用人群、合作医院范围（城市多选）、购买须知富文本（富文本编辑器）、上下架状态
- 保存：`POST /api/admin/packages` / `PUT /api/admin/packages/:id`；保存成功返回列表并刷新
- 异常：价格非法（≤0）→ 表单校验提示；保存失败 → message 错误提示

#### 核销管理（`/verify`，全医院）
- 筛选：医院（下拉，仅平台管理员可选，医院管理员锁定本院）、日期区间、核销状态
- 表格：核销码、订单号、套餐、用户（脱敏）、医院、核销人、时间
- 接口：`GET /api/admin/verify-records`；导出（可选）：blob 下载导出文件

#### 售卖管理（`/sales`，全医院）
- 筛选：医院、日期；统计卡片：销售额、订单数、退款数
- 表格：订单号、套餐、用户、金额、支付方式、状态、时间
- 接口：`GET /api/admin/sales`

#### 支付账单（`/bills`）
- **说明：微信支付在 C 平台小程序内完成；支付宝对公订单由支付宝同步至 C 平台**（后端轮询/回调写入），前端仅展示
- tab（el-tabs）：微信支付 / 支付宝
- 表格：账单号、关联订单、渠道、金额、交易状态、支付时间、同步时间、对账状态（已对账/差异）
- 接口：`GET /api/admin/bills { channel, page }`
- 差异账单行高亮红色并支持「重新同步」按钮（`POST /api/admin/bills/:id/sync`）

#### 医院管理（`/hospitals`，可选，仅平台管理员）
- 表格 CRUD：医院名、城市、地址、联系电话、可检测时间、状态（合作中/停用）
- 弹窗表单新增/编辑；删除需二次确认
- 接口：`GET/POST/PUT/DELETE /api/admin/hospitals`

---

## 6. 核心业务流程（前端视角）

### 6.1 登录注册流程
```
启动小程序
  ├─ 有 token 且未过期 → 首页
  └─ 无 token → 登录页
       ├─ 点击「微信一键登录」→ wx.login 拿 code
       ├─ 后端校验 openid
       │    ├─ 已关联账号 → 返回 token+user → 首页
       │    └─ 未关联 → 跳注册页（手机号+验证码）
       │         └─ 注册成功 → 自动关联微信 → 首页
       └─ 异常：拒绝授权 → 「未授权无法使用小程序」+重新授权
```

### 6.2 购买 → 支付 → 核销 → 结果 流程
```
套餐列表(筛选) → 套餐详情 → 立即购买 → 订单确认(总价)
  → 提交订单(创建订单+申请订阅消息) → wx.requestPayment
      ├─ 支付成功 → 后端回调 → 生成核销码 + 推送「购买成功」通知
      │    → 订单详情展示核销码
      └─ 支付失败/取消 → 提示「支付失败，请重新支付」，订单保留待支付
前往检测 → 合作医院查询(定位/筛选) → 选择医院
到医院出示核销码 → 医护扫码核销 → 核销接口更新状态 → 推送「已核销」
检测完成 → 医院上传结果 → C平台同步 → 推送「检测结果已出」 → 用户查看结果
```

### 6.3 抗凝用药管理流程
```
个人中心 → 用药管理 → 新增用药（药名/剂量/多时间点/订阅提醒）
  → 保存 → C平台生成用药记录
到达时间点 → C平台推送「请服用XX药物，每日XX剂量」 → 用户点击消息跳用药管理
  → 用户标记「已服用」 → 同步更新记录
用药满设定周期（或检测后医生评估确认血栓）→ C平台推送「抗凝药物效果检测套餐」
  → 用户点击消息 → 套餐详情 → 购买 → 走 6.2 检测流程
```

### 6.4 消息通知机制
- **消息来源**：全部由 C 平台推送（订单/核销/结果/用药/套餐/系统），小程序**不本地存储消息**
- **触达双通道**：
  1. 微信订阅消息（一次性模板，需用户提前订阅授权）——用于强提醒
  2. 站内消息中心（长期可查、可删除）——用于回看
- **红点联动**：未读数 → 登录返回/首页 onShow 时拉取 → 更新 tabBar badge 与首页红点
- **类型化跳转**：消息详情按 `targetType + targetId` 跳对应页面（见消息详情页）

---

## 7. 数据模型与接口约定

### 7.1 核心数据模型（前端字段视图）

```
User          id, openid, unionid, phone, nickname, avatar, gender, age, height, weight,
              role(user/staff/hospital_admin/admin), hospitalId, status
Package       id, name, price, cover, images[], items[{name,desc}], targetPopulation[],
              cities[], hospitalCount, salesCount, notice, status(on/off), createdAt
Order         id, orderNo, userId, packageId, packageName, amount, payAmount, status
              (pending_pay/paid/verified/completed/cancelled), payChannel(wx/alipay),
              payTime, verifyCode, hospitalId, verifyTime, createdAt
VerifyRecord  id, code, orderId, packageId, hospitalId, staffId, verifyTime, status
TestResult    id, orderId, userId, packageId, hospitalId, reportItems[{name,value,unit,range,abnormal}],
              reportUrl, status(pending/uploaded/published), uploadedAt, publishedAt
Medication    id, userId, drugName, dosePerTime, timesPerDay, timePoints[{id,time}],
              reminderOn, status(active/stopped), startAt, endAt, doctorAssessed(normal/thrombosis)
MedicationRecord id, medicationId, timePointId, date, status(taken/untaken)
Message       id, userId, type(order/medication/result/package/system), title,
              content, isRead, targetType, targetId, createdAt
PayBill       id, orderId, channel(wx/alipay), tradeNo, amount, status(success/fail/refund),
              paidAt, syncAt, reconcileStatus(ok/diff)
Hospital      id, name, address, phone, city, lat, lng, detectTime, status
```

### 7.2 接口清单（前端需对接，后端统一 `{ code, message, data }`，`code=0` 成功）

| 模块 | 接口 | 说明 |
|------|------|------|
| 登录 | `POST /api/auth/wechat-login` | code 换登录态，返回 isRegistered |
| 注册 | `POST /api/auth/send-code` / `POST /api/auth/register` | 短信验证码 / 注册并登录 |
| 用户 | `GET/PUT /api/user/profile` | 查询/更新档案（含性别年龄身高体重） |
| 套餐 | `GET /api/packages` `GET /api/packages/:id` | 列表（筛选分页）/详情 |
| 字典 | `GET /api/dicts/items` `GET /api/dicts/cities` | 检测项目/城市 |
| 订单 | `POST /api/orders` `GET /api/orders` `GET /api/orders/:id` `POST /api/orders/:id/cancel` | 创建(返回支付参数)/列表/详情/取消 |
| 支付回调 | `POST /api/payment/wechat-callback` | 微信支付回调（后端） |
| 医院 | `GET /api/hospitals` | 医院列表 |
| 核销 | `POST /api/verify/check` `POST /api/verify/confirm` `GET /api/verify/records` | 医护端校验/核销/记录 |
| 结果 | `GET /api/results` `GET /api/results/:id` `POST /api/results/upload` | 列表/详情/医院上传 |
| 用药 | `POST/GET /api/medications` `PUT/DELETE /api/medications/:id` `POST /api/medications/:id/records` | 用药 CRUD + 打卡 |
| 消息 | `GET /api/messages` `GET /api/messages/:id` `PUT /api/messages/:id/read` `DELETE /api/messages/:id` `GET /api/messages/unread-count` | 消息中心 |
| 首页 | `GET /api/home` | 聚合数据 |
| 医护 | `POST /api/staff/login` `GET /api/staff/statistics` | 医护端 |
| 管理 | `/api/admin/packages(CRUD)` `/api/admin/packages/:id/status` `/api/admin/verify-records` `/api/admin/sales` `/api/admin/bills` `/api/admin/bills/:id/sync` `/api/admin/statistics` `/api/admin/hospitals(CRUD)` | 管理端 |

### 7.3 统一错误码约定
| code | 含义 | 前端行为 |
|------|------|---------|
| 0 | 成功 | 正常处理 |
| 401 | 未登录/token 失效 | 清登录态 → 跳登录页 |
| 403 | 无权限 | toast 后端 message |
| 404 | 资源不存在 | 空态提示 |
| 1001 | 验证码错误 | toast「验证码错误，请重新输入」 |
| 1002 | 手机号已注册 | 引导回登录页 |
| 2001 | 订单不可支付（已支付/已取消） | toast 提示并刷新 |
| 2002 | 核销码无效/已使用/已过期 | 按核销端提示处理 |
| 500 | 服务异常 | toast「请稍后重试」 |

---

## 8. 异常处理与边界情况（汇总）

| 场景 | 用户提示 | 前端行为 |
|------|---------|---------|
| 用户拒绝微信授权 | 「未授权无法使用小程序」 | 展示「重新授权」按钮，点击重试登录 |
| 登录接口调用失败 | 「登录失败，请重试」 | 按钮恢复可点击，不阻塞重试 |
| 手机号验证码错误 | 「验证码错误，请重新输入」 | 清空验证码输入框，聚焦 |
| 支付失败/取消 | 「支付失败，请重新支付」 | 保留订单（待支付），订单详情可再次支付 |
| 核销失败（医护端） | 「核销失败，请联系医院工作人员或客服」 | 展示失败原因，支持重新核销/手动输入 |
| 检测结果未同步 | 「检测结果暂未更新，请稍后重试」 | 空态 + 刷新按钮 |
| 用药信息保存失败 | 「信息保存失败，请重试」 | 保留已填内容 |
| 用药提醒未推送 | 「用药提醒设置失败，请重新设置」 | 检查订阅授权，引导重新开启 |
| 用药信息修改未同步 | 「信息已更新，提醒时间将按新设置执行」 | 保存成功 toast 提示 |
| 网络异常 | 「网络异常，请检查网络」 | 请求封装统一处理；列表页支持下拉刷新 |
| 空数据 | 「暂无xx」 | `empty-state` 组件 + 操作按钮 |
| 敏感操作（删除/取消订单/退出登录） | 确认弹窗 | `wx.showModal` 二次确认 |

---

## 9. 合规与安全

1. **隐私政策与用户协议**：首次启动弹窗展示（`wx.privacy` 隐私协议 + 自定义弹窗），不勾选不进入；小程序后台配置《隐私保护指引》
2. **头像昵称**：使用微信「头像昵称填写能力」（`open-type="chooseAvatar"` + `type="nickname"`），不使用已废弃的 `wx.getUserProfile` 强制授权
3. **手机号**：优先使用 `open-type="getPhoneNumber"` 快速验证（需企业认证），降级方案为短信验证码
4. **定位**：仅医院查询页使用，页面需有明确用途文案；隐私授权被拒时降级为城市手动选择
5. **检测结果法律风险（重要，待确认）**：
   - 前端检测结果页必须展示免责声明：「结果仅供健康管理参考，不构成医疗诊断」
   - 购买流程需用户勾选「检测服务知情同意书」
   - 检测结果上传功能（医护端）**预留接口但默认隐藏/灰度**，待法务确认后开启；上线前需与 C 平台确认医疗数据合规要求（数据存储、传输加密、用户授权留痕）
6. **支付安全**：所有支付参数由后端生成，前端不参与签名；核销码展示需登录态校验；订单取消/退款以后端校验为准
7. **内容安全**：用户上传的头像、自定义药物名等建议过 `security.msgSecCheck` 内容安全接口（可选）

---

## 10. 生成前端代码时的注意事项

1. **工程**：生成两个独立小程序工程（用户端/医护端）+ 一个 Vue 管理端工程（`admin/`）；小程序共享组件与 utils 逻辑可复制，`project.config.json` 的 appid 留空占位由开发者填写；Vue 管理端按 1.3-B 与 2.7 规范实现（Vue 3 + Vite + Router + Pinia + Element Plus，axios 统一封装，接口约定与小程序一致）
2. **app.json 必须项**：pages 注册、window（导航栏标题/背景色 `#FFFFFF`/文字黑）、tabBar（仅用户端 4 个 tab）、`lazyCodeLoading: "requiredComponents"`（性能）、permission（定位用途说明）
3. **适配**：所有页面适配 iPhone 底部安全区；列表页统一 `onReachBottom` 分页 + `enablePullDownRefresh`
4. **性能**：setData 最小化（路径更新 + 拆分）；图片统一 `mode="widthFix"` + 懒加载；列表虚拟化（数据量大时）
5. **占位**：所有图片资源（banner、图标、封面）使用本地占位图，标注替换位置；后端域名 `https://api.c-platform.com` 为占位，需替换为真实域名并配置合法域名白名单
6. **订阅消息模板 ID**：统一放在 `utils/subscribe.js` 常量区，占位后由后端提供真实模板 ID
7. **样式**：严格使用 2.1 主题色 token（建议在 `app.wxss` 定义 CSS 变量），不引入第三方 UI 库，全部自定义组件保证一致性
8. **文档范围**：本文档聚焦前端；后端接口字段以 C 平台实际返回为准，前端按 7.2 约定对接，字段差异通过适配层（services）消化
9. **适老化强制项**：全站执行 2.1「适老化专项规范」；所有页面字号、点击热区、对比度严格按该规范实现，全站不允许出现小于 `28rpx` 的文字

---

## 附录：页面清单速查（生成代码时对照）

**用户端（18 页 + 4 tab）**
login, register, index(tab), package-list(tab), package-detail, order-confirm, order-list, order-detail, hospital-list, result-list, result-detail, medication-list, medication-edit, medication-detail, messages(tab), message-detail, profile(tab), profile-edit

**医护端（6 页）**
login, index, scan, verify-result, record, result-upload(预留)

**管理端（Vue Web，8 个路由）**
/login, /dashboard, /packages, /packages/edit/:id?, /verify, /sales, /bills, /hospitals(可选)
